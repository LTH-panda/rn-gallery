import {StyleSheet} from 'react-native';
import styled from 'styled-components/native';

export const styles = StyleSheet.create({
  styledInput: {
    backgroundColor: '#fff',
    borderRadius: 16,
    padding: 16,
  },
});

export const WriteFormView = styled.View``;
